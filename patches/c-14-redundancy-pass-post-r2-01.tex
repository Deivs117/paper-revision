% C-14 (renumbered from a colliding "C-10" -- see PROGRESS.md's coherence-audit note) --
% Redundancy/text-tightening pass run after R2-01's sweep had already closed
% (synced-to-overleaf). Targets prose that landed AFTER that sweep -- mainly the R3-06
% literature patch (commit 886e1db) -- plus one item outside R2-01's original scope.
% Audit copy of the change applied to sections/introduction.tex, sections/results.tex, and
% sections/conclusions.tex. The live files are the source of truth -- this is a record.
%
% Why not just re-run R2-01: that row already did 4 full passes across every section plus
% preamble.tex and is marked complete. A blind 5th pass would mostly re-find nothing. This
% pass grepped for literal repeated phrases instead of re-reading for subjective redundancy,
% and focused on the R3-06 patch's own new prose plus one 4x-repeated boilerplate sentence
% R2-01 never touched.
%
% Fix 1 -- introduction.tex: R3-06's own patch (commit 886e1db) introduced a duplicated
% argument across two paragraphs it added in the same patch. The table-introducing sentence
% (line 9) and the CPG paragraph (line ~30) both independently argued "feedback-modulated
% CPGs CAN arbitrate transitions elsewhere in the literature, but this work deliberately
% keeps that decision in the basal-ganglia stage instead" -- same claim, restated in
% different words ~20 lines apart. Kept the fuller justification (with the
% diagnose/validate-transitions rationale) in the CPG paragraph at line ~30; shortened line 9
% to state only the literature fact (CPGs can arbitrate) plus a forward pointer to where the
% "why we don't" argument lives, dropping the repeated clause from line 9.
%
% Fix 2 -- conclusions.tex vs results.tex: both files independently stated the near-verbatim
% clause "these constraints do not invalidate the results" -- results.tex's Limitations
% section closes with it, and conclusions.tex's limitations-pointer paragraph opened with the
% same clause before pivoting to future work. conclusions.tex's sentence was rewritten as a
% pure pointer to Section~\ref{ssec:Limitations} (which already carries that reassurance),
% dropping the repeated "do not invalidate" clause from conclusions.tex. results.tex is
% unchanged (it is where the claim actually belongs, alongside the limitations themselves).
%
% Fix 3 -- results.tex: the four back-to-back physical-scenario paragraphs (Appetitive,
% Aversive, Complex, Obstacle -- ssec:QuantReal) each opened with the identical sentence
% "Quantitative metrics are consolidated in Table~\ref{tab:consolidated_physical_metrics}."
% (confirmed via grep: exact string, 4 occurrences). Moved the table pointer into the
% subsection's own intro paragraph (once) and dropped it from all four per-scenario
% paragraphs, which now open directly with their scenario-specific finding.
%
% Adjacent finding, NOT fixed in this patch (filed separately as C-15): methodology.tex line
% 200 cross-references "the decision latency of approximately 47 ms measured across all
% experimental trials (Section~3)", but grep for "47" across results.tex finds no such
% figure anywhere in that section. This is a traceability/citation-accuracy gap, not
% something a redundancy pass fixes by cutting text -- OUTLINE.md had already flagged this
% number as unverified before this pass.
%
% Also fixed in the same pass, unrelated to text content: scripts/next_id.sh had a real bug
% -- bash arithmetic on a zero-padded ID like "09" is interpreted as octal and fails on the
% digit 9 (`value too great for base`). This broke ID allocation as soon as a 9th C-xx or
% N-xx row existed. Fixed with a `10#` base prefix (`NEXT=$((10#$MAX + 1))`); no PROGRESS.md
% content change, a script-only fix required to allocate this very row's ID (originally C-10,
% renumbered to C-14 -- see PROGRESS.md's coherence-audit note).
%
% No new claims were introduced by fixes 1-3 -- pure condensation of already-written prose.
% Net-new connective phrasing (the forward pointer in fix 1, the results-pointer rewrite in
% fix 2) wrapped in \revblue{} per the revision-convention hard rule; pure deletions (fix 3)
% needed no wrapping. validate_tex.sh / check_roundtrip.sh both pass.
