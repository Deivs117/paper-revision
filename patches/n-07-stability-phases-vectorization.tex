% PATCH: N-07
% Target section: none in sections/ -- assets/ only (F-08 changes image content, no
%   \label{}/\ref{} in results.tex, per R02-01-05 §6/§8)
% Requirement: intake/pending/R02-01-05_simulacion_grupo2_vectorizacion.md §3 (F-08/F-Data-03,
%   Group A), executed per the combined order in that document's §7 step 5.
%
% fig_stability_phases.png's underlying N=268 walk-cycle run was never archived to experiments/
%   (confirmed again this round -- no stability_log.csv anywhere has 268 rows). Per D-6 (still in
%   force), tab:crawl_stability's TR_mean=0.439 stays the source of truth for the aggregate
%   number; this patch only produces a code-traceable regeneration of the accompanying 6-panel
%   illustration.
%
% Method (new experiments/_plotting/extract/extract_stability_phases.py):
%   - Vectorized real geometry, not fabricated positions: stance-foot (green circle, RGB
%     ~(56,142,60)) and swing-foot (orange X, RGB ~(245,124,0)) marker pixel centroids,
%     color-masked and clustered, calibrated against each panel's own dashed -0.3..0.3m gridlines
%     -- confirmed all 6 panels share one global 1950 px/m scale. CoM is not extracted: by
%     construction this is a body-frame plot centered on base_link, so it sits at the origin in
%     every panel (confirmed visually).
%   - Found and excluded a color-mask false positive: the 4 swing-phase panels' "TR = x.xxx"
%     label box uses a green tone close enough to the stance-foot markers to leak 4-5 spurious
%     "feet" per panel from its border/text pixels -- fixed by dropping any green cluster with
%     data-y > 0.25 (real stance feet never appear there).
%   - Incenter + inradius (drawn only on the 4 triangular swing-phase panels) computed
%     analytically from the extracted triangle vertices (side-length-weighted incenter,
%     area/semiperimeter) rather than pixel-extracted -- exact given real vertices, no separate
%     detection pass needed for a derived quantity.
%   - TR, SM, and each panel's timestamp/phase-name read directly off the figure's own plain-text
%     labels (not calibrated pixel-graph values): A/t=120.46s/LU Swing Phase/TR=0.706/SM=2.8cm;
%     B/t=121.06s/4-Leg Stance; C/t=121.40s/RD Swing Phase/TR=0.748/SM=2.5cm; D/t=121.91s/RU
%     Swing Phase/TR=0.774/SM=2.2cm; E/t=122.42s/4-Leg Stance; F/t=122.75s/LD Swing
%     Phase/TR=0.746/SM=2.5cm.
%
% Re-plotting: new experiments/_plotting/builders/stability_phases.py, drawing the polygon
%   (convex-hull-ordered stance feet), CoM cross, swing-foot marker, incenter + inscribed circle
%   (triangular panels only), and the TR/"Static stance" label boxes matching the original's
%   style. Visually compared side-by-side against the original PNG -- polygon shapes, marker
%   positions, incenter circles, and all text labels match panel-for-panel.
%
% Promoted to assets/ with --force (same filename -- F-08 only changes image content, confirmed
%   no \label{}/\ref{} change needed in results.tex per R02-01-05 §6/§8).
%
% Verification: no LaTeX changes in this patch (image-only); validate_tex.sh/check_roundtrip.sh
%   re-run as part of the same working-tree state, both pass.
% Status: applied
