% C-53 -- NEU_IMU/NEU_IMU2 "Locomotion 2" panel removal (redundancy_closing_audit_consolidation_2026-09-02.md, [FIG-X17])
% Audit copy only -- no sections/*.tex text changed by this patch (image regeneration only). No
% \revblue{} applicable since no prose changed.
%
% Origin: surfaced while the author reviewed the current NEU_IMU/NEU_IMU2 images sent for the
% Group-2 figure-merge decision (figure_regeneration_candidates.md, FIG-2a, declined). Initial
% premise ("neuron X17 never fires") was checked against experiments/_plotting/vectorized/
% neu_imu.csv and neu_imu2.csv BEFORE any change: X17 is tonically active near-constant
% (activation_normalized 0.502-1.0, mean 0.503, 731 samples in each CSV), not silent -- corrected
% to the author before acting. Author decision, with the corrected fact in hand: drop the panel
% anyway, since it carries no interpretable temporal variation for the reader (a real but
% deliberate simplification, not a data-error correction).
%
% Code changes (not LaTeX -- see git diff for this commit):
%  - experiments/_plotting/builders/imu_terrain_real.py::build_neu_imu(): added an optional
%    `panels` parameter (defaults to the original 4-panel list, so NEU_IMU_real/NEU_IMU2_real's
%    __main__ call sites are byte-for-byte unaffected); group-title/letter placement rewritten to
%    derive its axes boundaries from the actual panel list passed in, not hardcoded indices, so it
%    works for a 3-panel (or any) subset.
%  - experiments/N-01-simulation-figure-regeneration/scripts/regenerate_neu_panels.py: NEU_IMU/
%    NEU_IMU2 call sites now pass panels=["Locomotion_1", "Decision_1", "Decision_2"].
%
% Verification before promoting:
%  - Re-ran regenerate_neu_panels.py in full; NEU_EST_UNIB/UNIG/MUL.png regenerated byte-identical
%    to the currently promoted assets (cmp), confirming the other 3 outputs were unaffected.
%  - Visually inspected both new NEU_IMU.png/NEU_IMU2.png (3 panels, a)/b) letters and group titles
%    correctly positioned) before scripts/promote_figure.sh --force.
%  - compile_pdf.sh re-run clean; rendered page (55, fig:NEU_IMU) visually confirmed to match.
%  - Confirmed via grep that no results.tex prose references X17 or "Locomotion 2" by name -- no
%    caption/prose rewording needed.
%
% assets/NEU_IMU.png and assets/NEU_IMU2.png replaced in place (same filenames, same
% \includegraphics calls in sections/results.tex -- no LaTeX edit needed, hence no diff block
% below).
