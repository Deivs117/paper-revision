# DISCARDED — not the canonical "Complex" physical scenario source

Confirmed by the author 2026-08-27 (Informe 2, decision D-2): `MultiRGB_t01..t03` (activates all
three stimulus colors: red, green, blue) is the canonical source for `Complex_Real_Plot_*.png` and
`NEU_EST_MUL_real.png` — matching the "Complex Multi-Stimulus" scenario's 3-stimulus design.

This folder (`MultiRB_*`, only red+blue, no green) is **not used for any published figure**. Kept
as historical reference only — likely an earlier/preliminary 2-stimulus version of the same test.
Do not use as a data source without re-confirming with the author first.

See `intake/pending/R02-01_data_traceability_and_plotting_plan.md` §0.1 (D-2) and
`experiments/real/MANIFEST.md`.
