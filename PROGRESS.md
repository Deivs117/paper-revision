# PROGRESS.md — Unified Change Tracking

One row per change, of any origin (`R2-xx`/`R3-xx` reviewer items, `N-xx` new content, `C-xx` corrections). See
README.md §6. Status values: `pending` → `drafted` → `applied` → `synced-to-overleaf`.

`Target section(s)` below reflects a full read of all four sections (`introduction.tex`, `methodology.tex`,
`results.tex`, `conclusions.tex` — see `OUTLINE.md` for details).

`Data source` is `—` when a row is pure prose/reorganization. When a row needs a figure/number that doesn't
exist yet, it points at `experiments/<ID>-<slug>/` — see README.md §9 / `experiments/README.md` for that
workflow (generation scripts → `output/` → `scripts/promote_figure.sh` → `assets/`).

`Category` is one of `Escritura` (prose/text editing), `Pruebas` (experiments/ablations/comparisons to run),
`Métricas` (new measurements/quantitative analysis). `Owner` is the person actually doing the work — used to
build the traceability dashboard (`docs/index.html`, see README.md §12); `scripts/build_dashboard.py` reads
both directly from this table, so keep them current when reassigning or picking up a row.

| ID | Reviewer | Requirement (short) | Target section(s) | Data source | Category | Owner | Status | Patch file |
|---|---|---|---|---|---|---|---|---|
| R2-01 | 2 | Condense repetitive text / remove duplicate plots / merge metric definitions / shorten warehouse sim section | results.tex: 12 edits, 9668->8524 words (9 first-pass edits + 3 deeper 2nd-pass edits: QuantReal physical-scenario 3-paragraph-per-scenario template condensed into 1 paragraph + new consolidated Table~tab:consolidated_physical_metrics mirroring the sim-side table; dropped duplicate mu-value restatement between FigReal_Terrain_Latencies_4Panels caption and its two prose paragraphs; dropped the 3rd redundant restatement of the ~47ms decision-latency figure in the Limitations paragraph, kept the other 2 as the establishing mention + self-contained table-caption anchor). methodology.tex: 5 edits, 5280->5625 words (3 first-pass edits: table dedup, notation dedup, Ar=28.0 re-citation cut; 2 deeper 2nd-pass edits: condensed the near-verbatim stimulus-to-mode correspondence restated ~250 lines apart between "Neuronal Control System" intro and "Gait Decision Module" intro into a cross-reference; condensed the back-to-back duplicate "fixed and uniform weights, per Sarvestani" claim in Obstacle Sensory Module into a single citation). conclusions.tex: 2 edits, 1622->1309 words (1st-pass edit merged triple-restated closing summary + split extensibility/future-work paragraphs; 2nd deeper-pass edit consolidated "future work" from 3 fragmented paragraphs -- Limitations pointer, extensibility paragraph, final "In conclusion" paragraph -- each independently restating overlapping real-world/outdoor-validation and sensor-fusion recommendations, into a single list in the extensibility paragraph). introduction.tex done on 2nd-pass re-read (2 edits, 1203->1193 words: dropped back-to-back duplicate Gurney2001a/b citation, condensed restated interpretable-vs-black-box/novelty argument into a cross-reference). All four sections now covered across two full passes, including the deeper 2nd-pass sweep. R2-01 fully complete. 3rd-pass cross-check found one more: R3-03's (Deivi's) new ServoCompensation subsection independently re-argued the same "static stability by construction, one-leg-airborne" claim already made with quantitative backing in results.tex's Quantitative Stability Analysis subsubsection (Table~tab:crawl_stability) — condensed to a cross-reference, see R3-03 row and patches/r2-01-19-servocompensation-crossref.tex. 4th-pass swept preamble.tex (never read before this round): the abstract's sentences 3-5 restated the same "robust/reliable adaptive switching" claim 3x with escalating vagueness; cut the purely-evaluative middle sentence and a redundant trailing clause (~190->~147 words, -23%, no facts lost). closing.tex checked too — bibliography/document-end boilerplate only, nothing to condense. | — | Escritura | Sebas | synced-to-overleaf | patches/r2-01-01..20-*.tex |
| R2-02 | 2 | Add FSM gait arbitration baseline comparison | methodology.tex (baseline description, new), results.tex (comparison data, new) — confirmed no existing FSM content anywhere | `experiments/R2-02-fsm-baseline/` (new FSM comparison data) | Pruebas | Diego, Sam | pending | — |
| R2-03 | 2 | Consolidate limitations into one dedicated section | Merged conclusions.tex's limitations paragraph (tuning/resilience/fusion/illumination) into results.tex §"Limitations of the Present Work" (Section~\ref{ssec:Limitations}) as a third paragraph; conclusions.tex now points to it with a short pointer sentence. Kept as a subsection of Results, not promoted to top-level `\section` — introduction.tex's "Section 2/3/4" roadmap unaffected. | — | Escritura | Deivi | synced-to-overleaf | patches/r2-03-consolidate-limitations.tex |
| R2-04 | 2 | Add multi-mode energy consumption + onboard MLP compute overhead analysis | MLP compute overhead: **already present** in results.tex ("Evaluation of the MLP-based terrain classifier" — per-sample latency vs SVM/RF/LogReg). Multi-mode long-term average energy: **new `tab:mode_steady_state_energy` added to results.tex** (Informe 1 §9.9, `patches/n-05-transition-energy-redaction.tex`) reporting mean steady-state power per mode, `robot_cmd`-segmented with the transition window excluded. Quadrupedal (C, N=20, $1.94\pm1.77$~W) and Differential Mobile (H, N=20, $1.36\pm0.98$~W) are real replicated measurements; Omnidirectional (X) is still a **single-segment N=1 preliminary estimate ($5.71$~W, no SD)**, flagged as such in the table caption/footnote — hardware team confirmed (2026-08-27) a dedicated Omnidirectional energy test battery is coming. **Not `applied` until that replacement lands** — see `experiments/R2-04-mode-energy/README.md`. | results.tex (new `tab:mode_steady_state_energy`, energy paragraph) | `experiments/R2-04-mode-energy/` | Métricas | Sam | drafted | `patches/n-05-transition-energy-redaction.tex` |
| R3-01 | 3 | Ablation analysis on basal ganglia arbitration network | methodology.tex §"Basal Ganglia Module" (~line 154, STN/GPe/GPi WTA circuit) for the ablation methodology; results.tex for the ablation results — confirmed no existing content, genuine gap | `experiments/R3-01-basal-ganglia-ablation/` | Pruebas | Diego | pending | — |
| R3-02 | 3 | Comparative tests removing neural layers/inhibitory connections to justify WTA over threshold logic | same as R3-01 — methodology.tex + results.tex, genuine gap, no existing content. Locomotion module already uses explicit threshold logic (20° frontal cone, `Ar=28.0`) — a natural in-paper contrast point | `experiments/R3-02-wta-vs-threshold/` | Pruebas | Diego | pending | — |
| R3-03 | 3 | Control/compensation strategies for low-torque servo constraints and open-loop gait instability | New `methodology.tex` §"Low-Torque Actuation Compensation Strategies" (`ssec:ServoCompensation`), added at the end of the section. Core 3 techniques from `Peter_arduino/src` firmware review: (1) statically-stable single-leg-swing wave gait (structural CoM/support-triangle guarantee), (2) rate-limited trajectory interpolation (bounds peak torque/current), (3) feed-forward tibia-angle offset compensating servo sag. 4 supporting measures mentioned briefly. IMU-based techniques deliberately excluded. 2026-08-26: Sam (firmware developer) confirmed `USE_IMU` was NOT active during the reported real-robot tests — exclusion is now permanent (citing it would misrepresent what ran on hardware), not pending. Sam also confirmed `TIBIA_BIAS_DEG=22°` is purely empirical, `Peter_arduino/src` is the exact firmware version tested, and no additional undocumented hardware-level compensation was applied — none of this adds new citable techniques; the 3 core techniques remain the full set. 2026-08-26 (Sebas, R2-01 3rd-pass redundancy sweep): technique (1)'s paragraph independently re-derived the same static-stability claim already made with quantitative backing in results.tex; condensed to a cross-reference to Table~tab:crawl_stability instead of re-arguing it — content/techniques unchanged. | prose-only, no `experiments/` needed | Escritura | Deivi | synced-to-overleaf | `patches/r3-03-servo-compensation-strategies.tex`, `patches/r2-01-19-servocompensation-crossref.tex` |
| R3-04 | 3 | Perception-decision noise robustness experiments (IMU drift, LiDAR noise, camera illumination distortion) | results.tex already has noise-robustness data across σ∈[0,4] for all 4 simulation scenarios — **verify whether this is generic sensor noise or specifically framed as IMU drift/LiDAR noise/illumination**, since the reviewer names specific noise sources | `experiments/R3-04-noise-robustness/` if the existing σ-sweep isn't framed per-sensor | Pruebas | Diego | pending | — |
| R3-05 | 3 | Inspection task performance metrics (tracking accuracy, coverage rate, autonomous re-planning) | results.tex — no coverage-rate or autonomous-replanning metrics found; genuine gap | `experiments/R3-05-inspection-metrics/` | Métricas | Sam | pending | — |
| R3-06 | 3 | Literature review comparative tables: Basal Ganglia vs CPG vs RL-based action selection | introduction.tex — **narrative comparison already exists** (threshold logic / MPC / RL vs basal ganglia), likely just needs converting to a table + reframing CPG as a third action-selection alternative (currently only discussed as a rhythm-execution layer) | — (table from existing text, no new data) | Escritura | Sebas, Deivi | pending | — |
| C-01 | — | Fix stale sentence in conclusions.tex: "Classification metrics for strengthening the MLP-based terrain classifier were also not evaluated" contradicts results.tex's own MLP evaluation subsection (accuracy/precision/recall/F1/ROC-AUC, ablation, classifier comparison already present) | conclusions.tex | — | Escritura | Sebas | synced-to-overleaf | patches/c-01-mlp-stale-limitation.tex |
| C-03 | — | *(Superseded by `C-06` — the file was never actually missing, `.gitignore` was silently excluding it from git; original diagnosis wrong.)* `\includegraphics{GRAPHICAL_ABSTRACT}` in preamble references an image missing from `assets/` (and from Overleaf, since assets/ mirrors it 1:1), blocking local compilation — commented out the `graphicalabstract` block until the image is supplied | preamble.tex | — | Escritura | Sebas | synced-to-overleaf | `patches/c-03-disable-graphical-abstract.tex` |
| C-06 | — | Supersedes C-03: GRAPHICAL_ABSTRACT was never actually missing — `.gitignore`'s blanket `*.pdf` rule (meant for compile byproducts) was silently excluding `assets/GRAPHICAL_ABSTRACT.pdf` from git, so it never reached GitHub/Sebas's clone even though it existed on disk, byte-identical to the Overleaf-tracked copy. Fixed `.gitignore` (`!/assets/**`), tracked the file, restored the `graphicalabstract` block in `preamble.tex` C-03 had commented out. | preamble.tex, assets/GRAPHICAL_ABSTRACT.pdf, .gitignore | — | Escritura | Deivi | synced-to-overleaf | `patches/c-06-restore-graphical-abstract.md` |
| C-05 | — | Review of Sebas's R2-01/R2-03/C-01/C-03 push (before Overleaf sync) found several patches with real prose rewrites left unwrapped, or `\revblue{}` fragmented around `\ref{}`/math — fixed wrapping across 18 paragraphs in `results.tex`/`conclusions.tex`; no content/numbers changed. Also corrected a stale "C-02" comment in `patches/c-03-disable-graphical-abstract.tex` left over from the C-04 reconciliation. | results.tex, conclusions.tex | — | Escritura | Deivi | synced-to-overleaf | `patches/c-05-revblue-consistency-fix.md` |
| C-04 | — | Strip all `\revblue{}` wrappers repo-wide (265 across preamble/introduction/methodology/results/conclusions) — last round's accepted changes become this round's plain baseline text, per new revision-round color convention (see OUTLINE.md / CLAUDE.md). *Originally logged as `C-02` on 2026-08-25; that ID was independently reused by a concurrent contributor working from a stale local copy of this file, which also overwrote this row and reset several `Data source` values above — reassigned to `C-04` and restored during reconciliation. Patch file kept its original name for git-history continuity.* | all `sections/*.tex` except `closing.tex` (0 occurrences there) | — | Escritura | Deivi | synced-to-overleaf | `patches/c-02-strip-revblue.md` |
| N-01 | — | **Closed 2026-08-27 (R02-01-04).** Reusable pipeline (`experiments/_plotting/`) that regenerates the noise-robustness figure grid (F-02 fusion), `FigA/B/C` (M-01–M-04 figures), and `tab:consolidated_simulation_metrics` from any simulation results folder. `familia_a_obstaculo` has 0/15 populated latency values anywhere in `experiments/` (corrects Informe 2's original F-Data-01 diagnosis) — author confirmed (2026-08-27) vectorizing the currently-published `fig1_Obstacle_macro.png` panel C as the fix for this round (`extract_fig1_obstacle_latency.py` → `vectorized/fig1_obstacle_latency.csv`; `macro_robustness.py` reads it with a fallback chain: vectorized CSV → real `metrics_raw.csv` (for a future re-run, D-12) → "no data" annotation). Also fixed a pre-existing title-clipping bug in `FigA_Stability_Profile.png` (`ecdf_phase_space.py`, unrelated to any number, found while reviewing before promotion). Promoted `fig1_noise_robustness_grid.png` (new, F-02 fusion of the 4 old `fig1_*_macro.png`, now `git rm`'d) + `FigA`/`FigB`/`FigC` (`--force`) to `assets/`. `results.tex`: collapsed the 4 scenario `figure*` blocks' `fig1_*_macro` subfigure into one new `figure*` (`\label{fig:noise_robustness_grid}`, 12 panels a–l, caption explicitly flags panel (i)/Obstacle-latency as vectorized, not measured); each scenario's `fig2_*_micro` (trial-005 temporal dynamics) demoted from subfigure to its own standalone `figure`, labels unchanged; updated the 4 Targeting paragraphs' `\ref{}`s to point at the new grid (by row) instead of the 4 removed labels. **New finding, out of scope for this row:** `tab_consolidated_simulation_metrics.csv` (recomputed from raw data by this same pipeline) does not match `tab:consolidated_simulation_metrics`'s published $T_{sim}$/Roll numbers (e.g. Obstacle Roll: 3.93±8.92° recomputed vs. 0.55±0.02° published) — not investigated or acted on here, flagged for a future pass; the table's LaTeX content was left untouched. All new/changed prose wrapped in `\revblue{}`. `validate_tex.sh`/`check_roundtrip.sh` both pass. | results.tex (4 scenario `figure*`→`figure` splits, new `fig:noise_robustness_grid`, 4 Targeting paragraphs) | `experiments/N-01-simulation-figure-regeneration/` (figures), `experiments/_plotting/vectorized/` (Obstacle panel) | Métricas | Deivi | applied | `patches/n-01-f02-grid-obstacle-latency-vectorization.tex` |
| N-02 | — | Physical-section figure regeneration (Tarea 3). Round 1: CSV rebuild + vectorization of all 22 published figures (see prior note). **Round 2 (2026-08-27, after author audit via local tool, notes in `intake/pending/n02_audit_notes_2026-08-27.md`):** imported `matplotlib`+`scientific-visualization` Claude Code skills into `.claude/skills/` (Okabe-Ito palette, no default grid — fixes the "quitar grilla" requests structurally, not per-figure); removed internal task-ID references ("F-06", vectorized/README.md pointer) from figure text; removed circular markers from ECDF plots; **removed `*_Real_Plot_2_Basal_Ganglia_Dynamics.png` entirely** (12 fewer figures) answering the reviewer's "cut repetitive... duplicated neural raster plots" request, folded its info into `*_Real_Plot_3` via red/blue stimulus-presence background shading — `results.tex` rewrite for this is a separate later pass, not done yet; **vectorized `NEU_EST_UNIG/MUL_real`** (rejected aggregate-substitute approach abandoned, same pixel-intensity technique as `NEU_IMU_real` now generalized to multi-column 8/12-panel layouts); `FigReal_Morphological_Transition_Energy` rejected by author, 3 redesign alternatives proposed (bar-per-transition-direction / cropped-time-series / text-table), not yet built pending author's choice. 19 figures now in `output/`, still staged, not promoted — awaiting a second audit pass. 2026-08-27: all 19 promoted to `assets/` (see N-03) and `results.tex` updated to match — round complete. | results.tex (physical results subsection, §MLP classifier) | `experiments/N-02-physical-figure-regeneration/` | Métricas | Deivi | applied | — |
| N-03 | — | Promoted the 12 approved `experiments/N-02-physical-figure-regeneration/output/` figures to `assets/` (`scripts/promote_figure.sh --force`, replacing the prior version of each) plus the new `fig_classifier_tradeoff_fused.png` (F-03 fusion, no prior asset). `sections/results.tex`: removed the `*_Real_Plot_2_Basal_Ganglia_Dynamics.png` subfigure from all 4 physical-scenario `figure*` blocks (Appetitive/Aversive/Complex/Obstacle), re-lettered the remaining 2 panels (a)/(b) and rewrote each external caption's (b) description to cover the now-composite neural+kinematic panel (Informe 1 §9.2); replaced the separate `fig5_classifier_comparison.png`/`fig6_computational_cost.png` figure blocks with a single `fig_classifier_tradeoff_fused.png` figure under `\label{fig:classifier_tradeoff}`, updating both `\ref{}` call sites (Informe 1 §9.3); corrected the SVM-RBF/Random Forest F1 figures cited in prose (0.900/0.995 → 0.857/0.947) to match the vectorized `classifier_fig5.csv` extraction — **author confirmed (2026-08-27) the vectorized/figure values are correct and the old prose values were stale**, same pattern as M-01–M-04 (see N-04). Removed the 4 now-unreferenced `*_Real_Plot_2_*.png` assets and the superseded `fig5_classifier_comparison.png`/`fig6_computational_cost.png` via `git rm`. All new/changed prose wrapped in `\revblue{}`. `validate_tex.sh`/`check_roundtrip.sh` both pass. | results.tex (4 physical-scenario `figure*` blocks, §MLP classifier figures/prose) | `experiments/N-02-physical-figure-regeneration/` (figure basis; see `experiments/_plotting/vectorized/README.md` for the classifier F1 vectorization) | Escritura | Deivi | applied | `patches/n-03-physical-figure-promotion-classifier-fusion.tex` |
| N-04 | — | Rewrote the 5 M-01–M-04 passages in `sections/results.tex` (Informe 1 §9.4, values verified in Informe 2 §0) to match the figures/raw data instead of the stale prose: Appetitive Targeting paragraph (decision latency is noise-dependent, ≈60 ms at $\sigma=0$ rising to 1.6–2.0 s, not a constant ≈47 ms) and the same claim removed from `tab:consolidated_simulation_metrics`'s caption and from the Limitations paragraph's cross-reference; Tipover Risk paragraph (flat $\mu_{TR}\approx0.58$–$0.60$ in both terrains, transient spike to $TR\approx0.77$ in rugged terrain, no monotonic escalation, replacing $\mu_{TR}\approx0.42$/$0.58$); $T_{switch}$/Heaviside paragraph and its figure caption (rugged terrain: sub-ms in 13/15 trials, 600 ms tail in 2/15 — not an invariant 0.3 ms; slope: 0.9–1.8 ms staircase, not a single value); phase-space paragraph and its figure caption (rugged roll≈1.59°/pitch≈1.67°, slope roll≈1.47°/pitch≈2.29°, Pitch-RMS separation with overlapping Roll RMS — not "0.43°/6.95°, zero cluster overlap"). Pure text/caption edits, no figure regeneration needed (figures were already correct — Informe 2 §0). All changes wrapped in `\revblue{}`. `validate_tex.sh`/`check_roundtrip.sh` both pass. | results.tex (Appetitive Targeting paragraph, `tab:consolidated_simulation_metrics` caption, Limitations paragraph, Tipover Risk paragraph, $T_{switch}$ paragraph + `FigB_Decision_Delay.png` caption, phase-space paragraph + `FigC_Terrain_Adaptability.png` caption) | — | Escritura | Deivi | applied | `patches/n-04-m01-m04-text-redaction.tex` |
| N-05 | — | Executed Informe 1 §9.7 (author-authorized 2026-08-27) for `FigReal_Morphological_Transition_Energy.png`: promoted the rewritten figure (`experiments/_plotting/builders/energy_transition.py`, now deriving power from `current_A`$\times$7.2V nominal NiMH voltage over 35 individually-detected `robot_cmd`-structural-command transitions, not an inferred power-peak) to `assets/` (`--force`, same filename); rewrote its caption and the `results.tex:934` paragraph with the real per-direction figures ($5.06\pm2.93$~J, $N=16$, Quadrupedal→Differential Mobile; $4.13\pm1.19$~J, $N=19$, Differential Mobile→Quadrupedal; Welch $t\approx1.18$, not significant) — dropped the old causal narrative (invariant Quad→Diff / dispersed Diff→Quad from river-pebble slippage), which the real data contradicts (SD is actually higher in Quad→Diff), replacing it with an unattributed observation of greater dispersion in that direction. All new/changed prose wrapped in `\revblue{}`. `validate_tex.sh`/`check_roundtrip.sh` both pass. | results.tex (`FigReal_Morphological_Transition_Energy.png` caption + energy paragraph) | `experiments/N-02-physical-figure-regeneration/` (figure), `experiments/_plotting/builders/energy_transition.py` (derivation) | Escritura | Deivi | applied | `patches/n-05-transition-energy-redaction.tex` |
| C-07 | — | Executed `intake/pending/R02-01-06_limpieza_editorial_menor.md` in full (§1 residual redundancy + §2 grammar/style, low priority, no blockers). §1: normalized `tab:consolidated_physical_metrics`'s unitless "$Var(z)$ peak >30,000" (Complex row) to a relative scale, "${\approx}\,27\times$ Obstacle baseline" (author confirmed this option over a footnote or leaving it as-is); the `FigReal_Terrain_Latencies_4Panels.png` 3-paragraph redundancy item was already resolved by an earlier pass (only 2 paragraphs remain, already condensed) — no change needed there. §2: reworded `results.tex:9`'s "non-certified test arena" phrasing (word-level, no `\revblue{}`); relocated the STL-mesh-discretization paragraph next to the inclined-slope test setup it actually describes instead of leaving it stranded after the transition-6 subsection (`\revblue{}`, structural move); merged the two near-identical real-vs-simulation appetitive-correction paragraphs into one (`\revblue{}`, structural fusion); the flagged >150-word Limitations run-on sentence turned out to already be split into 6 shorter sentences by an earlier pass — no change needed, noted as already resolved. Also surfaced and fixed (with author confirmation) a naming inconsistency beyond pure capitalization found while executing the "mode capitalization consistency" item: "Differential Rolling" (intro/methodology/simulation) vs. "Differential Mobile" (physical/hardware section, including the N-05 energy table/captions) were two names for the same mode, not case variants — unified all instances to "Differential Rolling" (author's choice) and applied consistent Title Case across `results.tex` for all three mode names (`Quadrupedal Walking`/`Differential Rolling`/`Omnidirectional` as proper nouns; lowercase as generic adjectives), including `tab:terrain_sequence`'s previously lowercase mode-name cells. `validate_tex.sh`/`check_roundtrip.sh` both pass. | results.tex (intro warehouse-sim sentence, STL-discretization paragraph, appetitive real-vs-sim paragraphs, `tab:consolidated_physical_metrics`, `tab:terrain_sequence`, all Differential Rolling/Mobile instances) | — | Escritura | Deivi | applied | `patches/c-07-limpieza-editorial-menor.tex` |
